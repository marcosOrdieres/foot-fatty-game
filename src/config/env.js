const ENV = {
    development: {
      env: 'development',
      authURL: 'http://localhost:3000/auth-api',
      apiURL: 'http://localhost:3000/api',
      scheme: 'http://',
    }
  };
  
  // const environment = ENV.development;
  const environment = ENV.development;
  
  export default environment
  